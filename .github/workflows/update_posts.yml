name: Update this repo's README
on:
  workflow_dispatch:
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: JasonEtco/rss-to-readme@v1
        with:
          feed-url: https://blog.wayofthepie.dev/rss.xml
          readme-section: feed
          path: .github/README.md
